import test from 'ava';
import React from 'react';
import { shallow } from 'enzyme';

import {{{name}}} from '{{{relativePath}}}../../src/components/{{{path}}}';
{{#styles}}
import classes from '{{{relativePath}}}../../src/components/{{{module}}}/styles';
{{/styles}}


test('{{{module}}} component', t => {
  t.fail('pending test');
});
