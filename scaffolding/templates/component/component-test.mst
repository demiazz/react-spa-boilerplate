import test from 'ava';
import React from 'react';
import { shallow } from 'enzyme';

import {{{name}}} from '../../src/components/{{{path}}}';
{{#styles}}
import classes from '../../src/components/{{{module}}}/styles';
{{/styles}}


test('{{{module}}} component', t => {
  t.fail('pending test');
});
