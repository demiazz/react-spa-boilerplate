import test from 'ava';
import React from 'react';
import { shallow } from 'enzyme';

import { mapStateToProps } from '{{{relativePath}}}../../src/components/{{{module}}}';
import { mapDispathToProps } from '{{{relativePath}}}../../src/components/{{{module}}}';
import { mergeProps } from '{{{relativePath}}}../../src/components/{{{module}}}';


test('{{{module}}} container', t => {
  t.fail('pending test');
});
