import React from 'react';
import { shallow } from 'enzyme';

import <%= name %> from '../../src/components/<%= path %>';
import { styleSheet } from '../../src/components/<%= path %>';


describe('components/<%= path %>', () => {
  // Write here tests for your component
});
